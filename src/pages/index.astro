---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const stats = [
  { value: 7, suffix: '+', label: 'Ani experiență', icon: '📐' },
  { value: 150, suffix: '+', label: 'Proiecte finalizate', icon: '🏗️' },
  { value: 2000, suffix: '+', label: 'Km drumuri proiectate', icon: '🛣️' },
  { value: 100, suffix: '+', label: 'Poduri proiectate', icon: '🌉' },
];

const services = [
  {
    icon: `<svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l5.447 2.724A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" /></svg>`,
    title: 'Proiectare drumuri',
    desc: 'Proiecte tehnice complete pentru drumuri comunale, județene și naționale — de la studii topografice și geotehnice la detalii de execuție.',
    href: '/servicii#proiectare-drumuri',
    color: 'from-blue-500 to-blue-700',
  },
  {
    icon: `<svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 17h18M3 17V9a2 2 0 012-2h4m10 10V9a2 2 0 00-2-2h-4m-4 0V5m0 2v10m0-10h4m-4 0H7" /></svg>`,
    title: 'Proiectare poduri, pasaje, pasarele',
    desc: 'Proiectare structuri de traversare pentru orice tip de obstacol — cursuri de apă, căi ferate, artere rutiere.',
    href: '/servicii#proiectare-poduri',
    color: 'from-emerald-500 to-emerald-700',
  },
  {
    icon: `<svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0H5m14 0h2m-2 0h-2M5 21H3m2 0h2m0-16h6m-6 4h6m-6 4h6" /></svg>`,
    title: 'Proiectare lucrări de sprijin și consolidări',
    desc: 'Soluții tehnice pentru stabilizarea terenurilor instabile, consolidarea taluzurilor și protejarea infrastructurii existente.',
    href: '/servicii#lucrari-sprijin-consolidari',
    color: 'from-amber-500 to-amber-700',
  },
];

const process = [
  { step: '01', title: 'Analiză', desc: 'Evaluăm cerințele proiectului, studiem amplasamentul și identificăm soluțiile optime.', icon: '🔍' },
  { step: '02', title: 'Proiectare', desc: 'Elaborăm documentația tehnică completă cu respectarea tuturor normativelor în vigoare.', icon: '📐' },
  { step: '03', title: 'Sprijin obținere avize', desc: 'Întocmim documentații pentru obținerea avizelor și acordurilor necesare de la autoritățile competente în concordanță cu Certificatul de Urbanism.', icon: '📋' },
  { step: '04', title: 'Livrare', desc: 'Predăm proiectul finalizat și oferim suport pe toată durata implementării.', icon: '🚀' },
];

const reasons = [
  { icon: '🏗️', title: 'Experiență dovedită', desc: 'Peste 7 ani în proiectarea infrastructurii, cu sute de proiecte livrate cu succes.' },
  { icon: '📋', title: 'Documentație completă', desc: 'Proiecte tehnice conforme cu legislația în vigoare, gata pentru autorizare și licitații.' },
  { icon: '⏱️', title: 'Respectăm termenele', desc: 'Livrare la termen, fără surprize. Planificare riguroasă și comunicare constantă.' },
  { icon: '🤝', title: 'Partener de încredere', desc: 'Colaborăm strâns cu autoritățile locale, constructorii și beneficiarii finali.' },
];
---

<style is:global>
  #hero-content {
    padding-top: 6.5rem; /* 104px: header 80px + 24px breathing room */
  }
  @media (min-width: 768px) {
    #hero-content {
      padding-top: 10rem;
    }
  }
</style>

<Layout title="Proiectare Infrastructură">
  <Header />

  <!-- HERO - Real photo + animated blueprint overlay -->
  <section class="relative min-h-screen flex items-start md:items-center overflow-hidden" id="hero" style="min-height: 100vh; min-height: 100svh">
    <!-- Real photo background with parallax -->
    <div class="absolute inset-0 hero-parallax">
      <img src="/img/hero-bg.png" alt="" class="absolute inset-0 w-full h-full object-cover scale-110" id="hero-bg" />
    </div>

    <!-- Gradient overlays -->
    <div class="absolute inset-0 bg-gradient-to-r from-brand-900/90 via-brand-900/70 to-transparent"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-brand-900 via-brand-900/30 to-brand-900/50"></div>

    <!-- Animated blueprint SVG overlay -->
    <svg class="absolute inset-0 w-full h-full pointer-events-none z-[2]" xmlns="http://www.w3.org/2000/svg">
      <!-- Blueprint grid -->
      <defs>
        <pattern id="bp-grid" width="80" height="80" patternUnits="userSpaceOnUse">
          <path d="M 80 0 L 0 0 0 80" fill="none" stroke="rgba(100,200,255,0.04)" stroke-width="0.5" />
        </pattern>
      </defs>
      <rect width="100%" height="100%" fill="url(#bp-grid)" class="hero-grid-fade" />

      <!-- Road trace line following the road in the photo -->
      <path d="M95%,95% Q80%,75% 70%,60% T55%,45% T45%,35% T35%,25%"
        fill="none" stroke="rgba(217,119,6,0.5)" stroke-width="2.5" stroke-linecap="round"
        stroke-dasharray="2000" stroke-dashoffset="2000"
        class="hero-trace" />

      <!-- Second road trace -->
      <path d="M60%,95% Q55%,80% 50%,70% T42%,55% T38%,40%"
        fill="none" stroke="rgba(100,200,255,0.25)" stroke-width="1.5"
        stroke-dasharray="1500" stroke-dashoffset="1500"
        class="hero-trace-2" />

      <!-- Measurement annotations -->
      <g class="hero-annotation" style="--delay: 3s">
        <line x1="60%" y1="55%" x2="75%" y2="55%" stroke="rgba(100,200,255,0.3)" stroke-width="0.8" />
        <line x1="60%" y1="53%" x2="60%" y2="57%" stroke="rgba(100,200,255,0.3)" stroke-width="0.8" />
        <line x1="75%" y1="53%" x2="75%" y2="57%" stroke="rgba(100,200,255,0.3)" stroke-width="0.8" />
        <text x="67.5%" y="53%" fill="rgba(100,200,255,0.35)" font-size="11" text-anchor="middle" font-family="monospace">1.240 m</text>
      </g>

      <!-- Roundabout annotation -->
      <g class="hero-annotation" style="--delay: 3.5s">
        <circle cx="48%" cy="42%" r="25" fill="none" stroke="rgba(217,119,6,0.3)" stroke-width="1.5" stroke-dasharray="4,4" />
        <text x="48%" y="38%" fill="rgba(217,119,6,0.35)" font-size="9" text-anchor="middle" font-family="monospace">R=22m</text>
      </g>

      <!-- Cross-section callout -->
      <g class="hero-annotation" style="--delay: 4s">
        <line x1="78%" y1="70%" x2="78%" y2="78%" stroke="rgba(100,200,255,0.2)" stroke-width="0.8" stroke-dasharray="3,3" />
        <circle cx="78%" cy="70%" r="3" fill="rgba(217,119,6,0.4)" />
        <text x="78%" y="81%" fill="rgba(100,200,255,0.3)" font-size="9" text-anchor="middle" font-family="monospace">Secțiune A-A</text>
      </g>

      <!-- Scale bar -->
      <g class="hero-annotation" style="--delay: 4.5s">
        <rect x="75%" y="88%" width="120" height="1" fill="rgba(255,255,255,0.2)" />
        <rect x="75%" y="87%" width="1" height="4" fill="rgba(255,255,255,0.2)" />
        <rect x="calc(75% + 120px)" y="87%" width="1" height="4" fill="rgba(255,255,255,0.2)" />
        <text x="calc(75% + 60px)" y="86%" fill="rgba(255,255,255,0.2)" font-size="9" text-anchor="middle" font-family="monospace">500 m</text>
      </g>
    </svg>

    <!-- Cross-section floating panel -->
    <div class="absolute bottom-24 right-6 md:right-12 z-[3] hidden md:block hero-cross-section">
      <div class="bg-brand-900/80 backdrop-blur-lg border border-white/10 rounded-2xl p-5 w-[320px]">
        <div class="flex items-center gap-2 mb-3">
          <div class="w-1.5 h-1.5 bg-accent-400 rounded-full animate-pulse"></div>
          <span class="text-white/40 text-[10px] font-mono uppercase tracking-wider">Profil transversal 1:50</span>
        </div>
        <svg viewBox="0 0 300 100" class="w-full" xmlns="http://www.w3.org/2000/svg">
          <path d="M0,85 Q40,83 80,80 Q140,75 150,74 Q160,75 220,80 Q260,83 300,85 L300,100 L0,100 Z"
            fill="rgba(120,80,40,0.3)" class="layer-reveal" style="--delay: 5s" />
          <path d="M70,80 L90,73 L120,68 L150,66 L180,68 L210,73 L230,80 L230,82 L210,76 L180,71 L150,69 L120,71 L90,76 L70,82 Z"
            fill="rgba(180,160,120,0.4)" class="layer-reveal" style="--delay: 5.5s" />
          <path d="M90,73 L115,68 L140,65 L150,64 L160,65 L185,68 L210,73 L210,76 L185,71 L160,68 L150,67 L140,68 L115,71 L90,76 Z"
            fill="rgba(160,170,180,0.5)" class="layer-reveal" style="--delay: 6s" />
          <path d="M105,68 L130,64 L145,62 L150,61 L155,62 L170,64 L195,68 L195,70 L170,66 L155,64 L150,63 L145,64 L130,66 L105,70 Z"
            fill="rgba(80,80,80,0.6)" class="layer-reveal" style="--delay: 6.5s" />
          <path d="M110,66 L135,62 L148,60 L150,59.5 L152,60 L165,62 L190,66 L190,68 L165,64 L152,62 L150,61.5 L148,62 L135,64 L110,68 Z"
            fill="rgba(50,50,50,0.8)" stroke="rgba(217,119,6,0.6)" stroke-width="0.8"
            class="layer-reveal" style="--delay: 7s" />
          <line x1="148" y1="59.5" x2="152" y2="59.5" stroke="rgba(255,255,255,0.7)" stroke-width="1.5"
            class="layer-reveal" style="--delay: 7.3s" />
          <line x1="110" y1="52" x2="190" y2="52" stroke="rgba(100,200,255,0.3)" stroke-width="0.5"
            class="layer-reveal" style="--delay: 7.5s" />
          <text x="150" y="49" fill="rgba(100,200,255,0.5)" font-size="8" text-anchor="middle" font-family="monospace" font-weight="bold"
            class="road-text" style="--delay: 7.7s">7.00 m</text>
          <!-- Layer labels -->
          <text x="240" y="88" fill="rgba(120,80,40,0.4)" font-size="5.5" font-family="monospace" class="road-text" style="--delay: 5.2s">Teren</text>
          <text x="240" y="77" fill="rgba(180,160,120,0.5)" font-size="5.5" font-family="monospace" class="road-text" style="--delay: 5.7s">Fundație</text>
          <text x="240" y="71" fill="rgba(160,170,180,0.6)" font-size="5.5" font-family="monospace" class="road-text" style="--delay: 6.2s">Bază</text>
          <text x="240" y="66" fill="rgba(100,100,100,0.6)" font-size="5.5" font-family="monospace" class="road-text" style="--delay: 6.7s">Binder</text>
          <text x="240" y="61" fill="rgba(217,119,6,0.6)" font-size="5.5" font-family="monospace" class="road-text" style="--delay: 7.2s">Uzură</text>
        </svg>
      </div>
    </div>

    <!-- Content -->
    <div id="hero-content" class="container relative z-[4] pb-20">
      <div class="max-w-2xl">
        <h1 class="text-[42px] md:text-[60px] lg:text-[80px] font-heading font-extrabold text-white leading-[1.05] mb-6" data-aos="fade-up">
          Construim
          <span class="relative inline-block">
            <span class="text-accent-400">fundația</span>
            <svg class="absolute -bottom-2 left-0 w-full" viewBox="0 0 200 12" fill="none">
              <path d="M2 10C50 2 150 2 198 10" stroke="#d97706" stroke-width="3" stroke-linecap="round"
                stroke-dasharray="200" stroke-dashoffset="200"
                class="animate-draw-path" style="animation-delay: 1s; animation-duration: 1.5s" />
            </svg>
          </span>
          <br />infrastructurii
          <br />de mâine
        </h1>

        <p class="text-white/60 text-[16px] md:text-[20px] max-w-xl leading-relaxed mb-2" data-aos="fade-up" data-aos-delay="100">
          Servicii integrate de proiectare (CFDP): drumuri, poduri, pasaje, pasarele, lucrări de sprijin și consolidări pentru toate fazele de proiectare.
        </p>
        <p class="text-white/80 text-[16px] md:text-[18px] max-w-xl leading-relaxed mb-10 font-medium" data-aos="fade-up" data-aos-delay="150">
          Experiență, precizie, rezultate.
        </p>

        <div class="flex flex-col sm:flex-row gap-4" data-aos="fade-up" data-aos-delay="200">
          <a href="/contact" class="group inline-flex items-center justify-center gap-2 bg-accent-500 text-white px-8 py-4 rounded-full font-semibold text-[15px] hover:bg-accent-600 transition-all hover:shadow-lg hover:shadow-accent-500/25 relative overflow-hidden">
            <span class="relative z-10">Solicită ofertă</span>
            <svg class="w-4 h-4 relative z-10 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
            <div class="absolute inset-0 animate-shimmer"></div>
          </a>
          <a href="/portofoliu" class="inline-flex items-center justify-center gap-2 bg-white/10 backdrop-blur-sm text-white px-8 py-4 rounded-full font-semibold text-[15px] border border-white/20 hover:bg-white/20 transition-all">
            Vezi portofoliu
          </a>
        </div>
      </div>

      <!-- Stats with counter animation -->
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-10">
        {stats.map((stat, i) => (
          <div class="group bg-white/5 backdrop-blur-sm border border-white/10 rounded-2xl p-3 md:p-6 text-center hover:bg-white/10 hover:border-white/20 transition-all duration-500 cursor-default"
            data-aos="fade-up" data-aos-delay={150 + i * 100}>
            <span class="text-[20px] md:text-[24px] block mb-1 md:mb-2">{stat.icon}</span>
            <div class="text-[26px] md:text-[40px] font-heading font-extrabold text-white counter-glow">
              <span class="counter" data-target={stat.value} data-suffix={stat.suffix}>0{stat.suffix}</span>
            </div>
            <div class="text-white/40 text-[13px] font-medium mt-1 group-hover:text-white/60 transition-colors">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- SERVICII with 3D tilt cards -->
  <section class="section-padding bg-steel-50 relative overflow-hidden" id="servicii">
    <!-- Background decoration -->
    <div class="absolute top-0 right-0 w-[400px] h-[400px] bg-brand-500/3 rounded-full blur-[100px]"></div>
    <div class="absolute bottom-0 left-0 w-[300px] h-[300px] bg-accent-500/3 rounded-full blur-[80px]"></div>

    <div class="container relative z-10">
      <div class="text-center max-w-2xl mx-auto mb-8">
        <span class="text-accent-500 font-semibold text-[13px] uppercase tracking-widest" data-aos="fade-up">Ce facem</span>
        <h2 class="text-[32px] md:text-[44px] font-heading font-bold text-brand-800 mt-3" data-aos="fade-up" data-aos-delay="50">
          Servicii de proiectare complete
        </h2>
        <p class="text-steel-500 text-[16px] mt-4 leading-relaxed" data-aos="fade-up" data-aos-delay="100">
          Oferim soluții complete de proiectare infrastructură, de la concept la implementare.
        </p>
      </div>

      <div class="grid md:grid-cols-3 gap-6">
        {services.map((service, i) => (
          <a href={service.href} class="group tilt-card glass-card rounded-2xl p-5 relative overflow-hidden" data-aos="fade-up" data-aos-delay={i * 100}>
            <div class={`absolute top-0 right-0 w-32 h-32 bg-gradient-to-br ${service.color} opacity-0 group-hover:opacity-10 rounded-full blur-[40px] transition-opacity duration-500 -translate-y-8 translate-x-8`}></div>
            <div class="tilt-inner relative z-10">
              <div class="w-14 h-14 bg-brand-500/10 rounded-xl flex items-center justify-center text-brand-500 group-hover:bg-accent-500 group-hover:text-white transition-all duration-300 group-hover:scale-110 group-hover:rotate-3" set:html={service.icon} />
              <h3 class="font-heading font-bold text-[18px] text-brand-800 mt-3 group-hover:text-accent-500 transition-colors">{service.title}</h3>
              <p class="text-steel-500 text-[14px] mt-2 leading-relaxed">{service.desc}</p>
              <div class="flex items-center gap-1 text-accent-500 font-semibold text-[14px] mt-3 translate-x-0 group-hover:translate-x-2 transition-transform">
                Detalii
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
                </svg>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- PROCES with animated timeline -->
  <section class="section-padding bg-brand-900 text-white relative overflow-hidden">
    <div class="container">
      <div class="text-center max-w-2xl mx-auto mb-8">
        <span class="text-accent-400 font-semibold text-[13px] uppercase tracking-widest" data-aos="fade-up">Cum lucrăm</span>
        <h2 class="text-[32px] md:text-[44px] font-heading font-bold text-white mt-3" data-aos="fade-up" data-aos-delay="50">
          Proces transparent, rezultate predictibile
        </h2>
      </div>

      <!-- Timeline desktop -->
      <div class="hidden md:block relative mb-8">
        <div class="absolute top-12 left-[12%] right-[12%] h-0.5 bg-white/10 rounded-full overflow-hidden">
          <div class="h-full bg-gradient-to-r from-accent-500 via-white/40 to-accent-500 rounded-full timeline-progress" style="width: 0%"></div>
        </div>
      </div>

      <div class="grid md:grid-cols-4 gap-8">
        {process.map((item, i) => (
          <div class="relative process-step" data-aos="fade-up" data-aos-delay={i * 150}>
            <div class="text-center md:text-left">
              <div class="relative inline-flex items-center justify-center w-20 h-20 rounded-2xl bg-white/5 border border-white/10 mb-5 mx-auto md:mx-0">
                <span class="text-[28px]">{item.icon}</span>
                <span class="absolute -top-2 -right-2 w-7 h-7 rounded-lg bg-accent-500 text-white text-[11px] font-bold flex items-center justify-center shadow-lg shadow-accent-500/30">{item.step}</span>
              </div>
              <h3 class="font-heading font-bold text-[18px] text-white">{item.title}</h3>
              <p class="text-white/50 text-[14px] mt-3 leading-relaxed">{item.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- HARTA ROMANIA -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <section class="section-padding bg-white" id="harta">
    <div class="container">
      <div class="text-center max-w-2xl mx-auto mb-6" data-aos="fade-up">
        <span class="text-accent-500 font-semibold text-[13px] uppercase tracking-widest">Prezență națională</span>
        <h2 class="text-[32px] md:text-[44px] font-heading font-bold text-brand-800 mt-3">
          Proiecte în toată România
        </h2>
        <p class="text-steel-500 text-[16px] mt-4">
          Am realizat proiecte de infrastructură în toate colțurile țării.
        </p>
      </div>
      <div id="romania-map" class="w-full rounded-2xl overflow-hidden shadow-lg border border-steel-100" style="height: 420px;" data-aos="fade-up" data-aos-delay="100"></div>
    </div>
  </section>

  <script is:inline>
  (function() {
    var projects = [
      {
        lat: 45.18, lng: 25.35,
        title: 'Modernizare drumuri de interes local',
        location: 'Comuna Aninoasa, Jud. Dâmbovița',
        categories: ['Drumuri'], faza: 'DALI+DTAC+PTE', year: '2025',
        slug: 'aninoasa-dambovita'
      },
      {
        lat: 45.03, lng: 24.97,
        title: 'Infrastructură Verde – Albastră',
        location: 'Comuna Bughea de Jos, Jud. Argeș',
        categories: ['Consolidări', 'Pasarele'], faza: 'SF+DTAC+PTE', year: '2025',
        slug: 'bughea-de-jos-arges'
      },
      {
        lat: 44.32, lng: 23.80,
        title: 'Închidere Inel de Trafic – Zona de Sud',
        location: 'Municipiul Craiova, Jud. Dolj',
        categories: ['Drumuri', 'Consolidări'], faza: 'DALI+DTAC+PTE', year: '2023–2025',
        slug: 'craiova-dolj'
      },
      {
        lat: 46.07, lng: 23.57,
        title: 'Amenajare Sens Giratoriu',
        location: 'Municipiul Alba Iulia, Jud. Alba',
        categories: ['Drumuri'], faza: 'DALI', year: '2024',
        slug: 'alba-iulia'
      },
      {
        lat: 47.62, lng: 23.50,
        title: 'Construire Pasaj „Clubul Văcarilor"',
        location: 'Județul Maramureș',
        categories: ['Drumuri', 'Poduri'], faza: 'SF', year: '2023',
        slug: 'pasaj-vacari-maramures'
      },
      {
        lat: 44.37, lng: 27.84,
        title: 'Amenajare Port Turistic Fetești',
        location: 'Municipiul Fetești, Jud. Ialomița',
        categories: ['Drumuri', 'Amenajări Hidrotehnice'], faza: 'SF', year: '2022',
        slug: 'port-turistic-fetesti'
      },
      {
        lat: 44.43, lng: 26.10,
        title: 'Pasaj pe A0 peste A2 Km 53+069',
        location: 'București',
        categories: ['Poduri'], faza: 'DE', year: '2020',
        slug: 'pasaj-a0-a2-bucuresti'
      },
      {
        lat: 45.76, lng: 22.77,
        title: 'Pasaj superior Gurasada–Simeria',
        location: 'Comuna Gurasada, Jud. Hunedoara',
        categories: ['Poduri'], faza: 'DE', year: '2020',
        slug: 'pasaj-gurasada-hunedoara'
      },
    ];

    function initMap() {
      if (!window.L || !document.getElementById('romania-map')) return;

      var map = L.map('romania-map', {
        center: [45.9, 24.8],
        zoom: 7,
        zoomControl: true,
        scrollWheelZoom: false,
      });

      L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> &copy; <a href="https://carto.com/">CARTO</a>',
        maxZoom: 18,
      }).addTo(map);

      var pinSvg = function(color) {
        return '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 42" width="32" height="42">' +
          '<path d="M16 0C9.373 0 4 5.373 4 12c0 9 12 28 12 28S28 21 28 12C28 5.373 22.627 0 16 0z" fill="' + color + '" stroke="#fff" stroke-width="2"/>' +
          '<circle cx="16" cy="12" r="5" fill="#fff"/>' +
          '</svg>';
      };

      projects.forEach(function(p) {
        var icon = L.divIcon({
          html: pinSvg('#1e3a5f'),
          className: '',
          iconSize: [32, 42],
          iconAnchor: [16, 42],
          popupAnchor: [0, -44],
        });

        var cats = p.categories.map(function(c) {
          return '<span style="background:#1e3a5f15;color:#1e3a5f;padding:2px 8px;border-radius:99px;font-size:11px;font-weight:600;">' + c + '</span>';
        }).join(' ');

        var popup = '<div style="font-family:\'Plus Jakarta Sans\',sans-serif;min-width:200px;max-width:260px;">' +
          '<div style="display:flex;gap:4px;flex-wrap:wrap;margin-bottom:6px;">' + cats +
          '<span style="background:#d9770615;color:#d97706;padding:2px 8px;border-radius:99px;font-size:11px;font-weight:600;">' + p.faza + '</span>' +
          '</div>' +
          '<strong style="font-size:14px;color:#0a1628;line-height:1.3;display:block;">' + p.title + '</strong>' +
          '<p style="font-size:12px;color:#64748b;margin-top:4px;">📍 ' + p.location + '</p>' +
          '<p style="font-size:12px;color:#94a3b8;margin-top:2px;">📅 ' + p.year + '</p>' +
          '<a href="/portofoliu#' + p.slug + '" style="display:inline-flex;align-items:center;gap:4px;margin-top:10px;background:#1e3a5f;color:#fff;padding:6px 14px;border-radius:99px;font-size:12px;font-weight:600;text-decoration:none;">Vezi proiect →</a>' +
          '</div>';

        L.marker([p.lat, p.lng], { icon: icon })
          .bindPopup(popup, { maxWidth: 280 })
          .addTo(map);
      });

      // Pulse animation on markers
      var style = document.createElement('style');
      style.textContent = '.leaflet-div-icon { background: none !important; border: none !important; } ' +
        '.leaflet-div-icon svg { filter: drop-shadow(0 4px 8px rgba(30,58,95,0.4)); transition: transform 0.2s; } ' +
        '.leaflet-div-icon:hover svg { transform: scale(1.15); }';
      document.head.appendChild(style);
    }

    // Load Leaflet JS then init
    if (!window.L) {
      var script = document.createElement('script');
      script.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
      script.onload = initMap;
      document.head.appendChild(script);
    } else {
      initMap();
    }
  })();
  </script>

  <!-- CTA with animated gradient -->
  <section class="section-padding bg-steel-50">
    <div class="container">
      <div class="relative bg-gradient-to-br from-brand-500 to-brand-700 rounded-3xl p-8 md:p-10 text-center overflow-hidden" data-aos="fade-up">
        <div class="absolute top-0 right-0 w-[300px] h-[300px] bg-accent-500/20 rounded-full blur-[80px] animate-morph"></div>
        <div class="absolute bottom-0 left-0 w-[200px] h-[200px] bg-brand-300/20 rounded-full blur-[60px] animate-morph" style="animation-delay: -4s"></div>

        <div class="relative z-10">
          <h2 class="text-[28px] md:text-[40px] font-heading font-bold text-white">
            Ai un proiect de infrastructură?
          </h2>
          <p class="text-white/60 text-[16px] mt-4 max-w-lg mx-auto">
            Contactează-ne pentru o consultanță gratuită. Analizăm cerințele și îți oferim o soluție personalizată.
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center mt-5">
            <a href="/contact" class="group inline-flex items-center justify-center gap-2 bg-accent-500 text-white px-8 py-4 rounded-full font-semibold text-[15px] hover:bg-accent-600 transition-all hover:shadow-lg relative overflow-hidden">
              <span class="relative z-10">Solicită consultanță gratuită</span>
              <svg class="w-4 h-4 relative z-10 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
              <div class="absolute inset-0 animate-shimmer"></div>
            </a>
            <a href="tel:+40700000000" class="inline-flex items-center justify-center gap-2 bg-white/10 backdrop-blur text-white px-8 py-4 rounded-full font-semibold text-[15px] border border-white/20 hover:bg-white/20 transition-all">
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
              </svg>
              Sună-ne acum
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<!-- Hero parallax on mouse + scroll -->
<script is:inline>
(function() {
  const hero = document.getElementById('hero');
  const heroBg = document.getElementById('hero-bg');
  if (!hero || !heroBg) return;

  hero.addEventListener('mousemove', (e) => {
    const rect = hero.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width - 0.5;
    const y = (e.clientY - rect.top) / rect.height - 0.5;
    heroBg.style.transform = `scale(1.1) translate(${x * -15}px, ${y * -10}px)`;
  });

  hero.addEventListener('mouseleave', () => {
    heroBg.style.transform = 'scale(1.1) translate(0, 0)';
  });

  window.addEventListener('scroll', () => {
    const scrollY = window.scrollY;
    if (scrollY < window.innerHeight) {
      heroBg.style.transform = `scale(1.1) translateY(${scrollY * 0.2}px)`;
    }
  });
})();
</script>

<!-- Animations script -->
<script is:inline>
  // Counter animation
  function animateCounters() {
    const counters = document.querySelectorAll('.counter');
    counters.forEach(counter => {
      const target = parseInt(counter.dataset.target);
      const suffix = counter.dataset.suffix || '';
      const duration = 2000;
      const start = performance.now();

      function update(now) {
        const elapsed = now - start;
        const progress = Math.min(elapsed / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 4);
        const current = Math.floor(eased * target);
        counter.textContent = current + suffix;

        if (progress < 1) {
          requestAnimationFrame(update);
        } else {
          counter.textContent = target + suffix;
        }
      }

      requestAnimationFrame(update);
    });
  }

  // Trigger counters when visible
  const statsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateCounters();
        statsObserver.disconnect();
      }
    });
  }, { threshold: 0.3 });

  document.querySelectorAll('.counter').forEach(el => {
    statsObserver.observe(el.closest('.grid'));
  });

  // Timeline progress animation
  const timelineObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const bar = entry.target.querySelector('.timeline-progress');
        if (bar) {
          bar.style.transition = 'width 2s cubic-bezier(0.4, 0, 0.2, 1)';
          bar.style.width = '100%';
        }
        timelineObserver.disconnect();
      }
    });
  }, { threshold: 0.3 });

  const timelineSection = document.querySelector('.timeline-progress');
  if (timelineSection) {
    timelineObserver.observe(timelineSection.closest('.relative'));
  }

  // 3D tilt effect on service cards
  document.querySelectorAll('.tilt-card').forEach(card => {
    card.addEventListener('mousemove', (e) => {
      const rect = card.getBoundingClientRect();
      const x = e.clientX - rect.left;
      const y = e.clientY - rect.top;
      const centerX = rect.width / 2;
      const centerY = rect.height / 2;
      const rotateX = (y - centerY) / 20;
      const rotateY = (centerX - x) / 20;
      card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(1.02)`;
    });

    card.addEventListener('mouseleave', () => {
      card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale(1)';
    });
  });

  // Parallax on scroll for floating elements
  let ticking = false;
  window.addEventListener('scroll', () => {
    if (!ticking) {
      requestAnimationFrame(() => {
        const scrollY = window.scrollY;
        document.querySelectorAll('.parallax-layer').forEach(el => {
          const speed = parseFloat(el.dataset.speed) || 1;
          el.style.transform = `translateY(${scrollY * speed * 0.05}px)`;
        });
        ticking = false;
      });
      ticking = true;
    }
  });
</script>
